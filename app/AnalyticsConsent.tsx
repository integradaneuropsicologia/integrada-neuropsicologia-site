"use client";

// Compatibility export for older imports. Consent Mode v2 is initialized in
// the root layout and this component only renders the unified preference UI.
export { CookieConsent as AnalyticsConsent } from "@/components/CookieConsent";
